Worker-offload example
======================

This is a node.js application that takes in data from Twitter and uses MQ Light to send it off to one or more workers for processing, allowing it to remain responsive.
